# Task: data/ source separation + data-encrypted/ output structure

## Request
- `data/` = plaintext source (gitignored entirely)
  - `data/projects/*.json`
  - `data/content/tasks/**/*.md`
  - `data/content/posts/**/*.md`
  - `data/content/descriptions/*.md`
- `pnpm encrypt` reads `data/` → writes:
  - `src/content/**/*.enc.md` (committed)
  - `data-encrypted/projects/*.enc.json` (committed)
- `pnpm decrypt` reads committed encrypted files → restores `data/`
- Build always uses encrypted files; can't accidentally build plaintext

## Sub-tasks
- [x] `scripts/encrypt.mjs` — new source/dest paths; don't delete originals in data/
- [x] `scripts/decrypt.mjs` — read from src/content + data-encrypted, write to data/; don't delete encrypted
- [x] `scripts/generate-fields.mjs` — change content paths to data/content/
- [x] `src/content.config.ts` — change glob to **/*.enc.md
- [x] pages (4 files) — glob to /data-encrypted/projects/*.enc.json
- [x] `.gitignore` — simplify to just `data/` + photo jpg ignores
- [x] `.dockerignore` — add data-encrypted/
- [x] `test-env/docker-compose.yml` — update mounts
- [x] `test-env/e2e/encryption-setup.ts` — write to data/content/ not src/content/
- [x] fixture files — create data/content/ fixtures; rename src/content fixtures to .enc.md; create data-encrypted fixture
- [x] `CLAUDE.md` + `HELP.md` — update docs

## Notes
- decrypt does NOT delete encrypted files (src/content + data-encrypted are committed)
- .dockerignore already excludes src/content and data — add data-encrypted/ too
- ID regex fix ((?:\.enc)?\.mdx?$) still needed for .enc.md IDs
- Test suite: non-encrypted service mounts fixture src/content (*.enc.md without enc_body) + fixture data-encrypted
