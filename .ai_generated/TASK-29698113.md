# TASK-29698113

**Request:** Read `PROJECT.md` and execute — analyze for problems/suggestions, propose file structure and architecture, generate `CLAUDE.md`.

**Date:** 2026-06-19

---

## Analysis: Problems & Ambiguities

### Critical

1. **Blog post YAML frontmatter undefined**: The spec defines the task YAML schema but never defines what fields a blog post MDX file should have. At minimum posts need: `id`, `title`, `published_at` (or `started_at`), `last_activity_at`. Blocking for content collection config.

Answer:
| Field name | Type | Description |
|---|---|---|
| id | String | the ULID of the blog post |
| title | String | the title |
| published_at | ISO 8601 | when the post was published |
| updated_at | ISO 8601 | (optional) when the post was edited after it was published |
| project_id | String | the project ID to which this blog post is linked |
| tags | String[] | the tags, all lower case, comma separated (should be validated at build) |


2. **Blog list page undescribed**: The project page says "the last blog component (as displayed on the blog list page)" but no "Blog list page" section exists in the spec. Need to infer its design.

Answer: the blog list page should have pagination, sorting, search by title, and each blog post component should we widgets-like, like the projects on home-page, but with no image and should contain the first paragraph of the post.

3. **"Active task" ambiguity on project page**: Spec says "the task that is active" — assumed to mean stage `started`. What if multiple tasks have stage `started`? Show the most recent (latest ULID)?

Answer: there will be always only one task per project that is active. I need
something additional to be done I will add it in the current active task.

### Non-critical / Typos

4. **Typo in task YAML**: `id` described as "the ULID of the project" — should be "the ULID of the task".

Answer: You are right;

5. **`last_activity_at` for blog posts**: The spec says "generated from the date of the last time the **task** file was updated" but this is under the blog posts section. Copy-paste error; means blog post file.

Answer: You are right;

6. **`last_activity_at` fallback**: Project JSON's `last_activity_at` should fall back to `started_at` if no tasks or posts exist yet.

Answer: You are right;

7. **Missing image fallback**: No spec for when `photos/home_page/[project_id].jpg` or `photos/covers/[project_id].jpg` is absent. Will use a placeholder/default image.

Answer: If no image then we should have a placeholder, download one for each
required case;

8. **Pagination page size**: Not specified — will default to 10 items per page.

Answer: Good;
Additional: the pagination should reflect in URL for both the projects page and
blog list page.

9. **Project description file**: MDX files in descriptions directory — assumed to have no required frontmatter (body-only MDX content).

Answer: Correct.

10. **Interactive features on static site**: Sorting, filtering, and pagination require client-side interactivity. Will use Astro React islands (`client:load`).

Answer: Alright.

---

## Proposed File Structure

```
/
├── .ai_generated/           # AI task tracking files
├── src/
│   ├── components/
│   │   ├── ProjectCard.tsx
│   │   ├── ProjectsGrid.tsx  # island: filter, sort, paginate
│   │   ├── BlogPostCard.tsx
│   │   └── TaskCard.tsx
│   ├── layouts/
│   │   └── BaseLayout.astro
│   ├── pages/
│   │   ├── index.astro
│   │   ├── projects/
│   │   │   └── [id].astro
│   │   ├── blog/
│   │   │   └── [projectId]/
│   │   │       └── [postId].astro
│   │   └── help.astro
│   ├── content/
│   │   ├── config.ts
│   │   ├── descriptions/    # [project_id].mdx — project long-form description
│   │   ├── tasks/
│   │   │   └── [project_id]/
│   │   │       └── [task_ulid].mdx
│   │   └── posts/
│   │       └── [project_id]/
│   │           └── [post_ulid].mdx
│   └── styles/
│       └── global.css
├── data/
│   └── projects/            # [project_id].json
├── public/
│   └── photos/
│       ├── home_page/       # [project_id].jpg
│       └── covers/          # [project_id].jpg
├── scripts/
│   └── generate-fields.ts   # pre-build: compute generated JSON fields
├── tests/
│   └── e2e/                 # Playwright tests
├── CLAUDE.md
├── HELP.md
├── PROJECT.md
├── astro.config.mjs
├── tsconfig.json
├── package.json
└── pnpm-lock.yaml
```

---

## Tasks

- [x] Create task file
- [x] Analyze PROJECT.md for problems and suggestions
- [x] Propose file structure
- [x] Propose architecture
- [x] Generate CLAUDE.md

---

## Notes

- Inferred blog post YAML: `id` (ULID), `title` (String), `project_id` (String), `started_at` (ISO 8601), `last_activity_at` (ISO 8601, generated)
- Blog list page inferred as: paginated list of post cards per project or globally, sorted by date
- Pagination default: 10 items/page
- Image fallback: public/photos/placeholder.jpg
