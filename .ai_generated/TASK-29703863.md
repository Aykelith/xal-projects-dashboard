# Remove plaintext excerpt generation from encrypt script

**Request:** Remove `excerpt` field generation from `scripts/encrypt.mjs` — it leaks first paragraph of encrypted content as plaintext. Not needed since `home_description` covers preview on home page.

## Sub-tasks

- [x] Remove excerpt generation block from `scripts/encrypt.mjs` (lines 111–116)
- [x] Remove `excerpt` from `src/content.config.ts` schema
- [x] Remove `excerpt` field from committed `src/content/descriptions/xal-ai-projects.enc.md`

## Notes

- Both `blog/index.astro` and `projects/[id].astro` already fall back to `entry.body` if no excerpt — encrypted files have empty body so excerpt becomes `''`; `BlogPostCard` already guards with `{post.excerpt && ...}` so no render change needed
- `types.ts` + `BlogPostCard.tsx` don't need changes — pages still pass `excerpt: ''`
