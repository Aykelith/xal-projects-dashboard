# TASK-29701008

## Request
Add non-encrypted data files to .gitignore. Move all test-related files to a `test-env/` directory with a Docker Compose setup for running Playwright tests.

## Sub-tasks

- [x] Create `test-env/` structure (playwright configs, e2e specs, fixtures)
- [x] Create `test-env/Dockerfile` and `test-env/docker-compose.yml`
- [x] Create `.dockerignore` at project root
- [x] Move test data to `test-env/fixtures/`
- [x] Update `.gitignore` (add `data/` and `src/content/`)
- [x] Update `package.json` scripts to use docker compose
- [x] Delete old `playwright.config.ts`, `playwright.encryption.config.ts`, `tests/` from root
- [x] Delete old test data from `data/` and `src/content/`
- [x] Update `CLAUDE.md`

## Notes
- `encryption-setup.ts` ROOT computed as `../../` from its location — works the same from `test-env/e2e/`
- Encryption tests mount fixtures rw (setup modifies content), regular tests mount ro
