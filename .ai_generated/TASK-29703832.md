# Task: Render Markdown in encrypted project descriptions

## Original request
Add client-side Markdown→HTML parsing for encrypted description content (EncryptedContent component).

## Sub-tasks
- [x] Security check for `marked` package
- [x] Install `marked`
- [x] Add `type="markdown"` to EncryptedContent, parse with marked before dangerouslySetInnerHTML
- [x] Update `[id].astro` to use `type="markdown"` for description

## Notes
- Non-encrypted path uses Astro's `render()` — already correct
- Encrypted path was using `type="text"` → rendered as `<pre>` (raw Markdown string)
