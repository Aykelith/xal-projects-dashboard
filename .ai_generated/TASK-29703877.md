# Task: Add `on_hold` project stage

**Request:** Add new stage value `on_hold` — project stopped temporarily, not abandoned.

## Sub-tasks

- [x] `src/types.ts` — add `on_hold` to `ProjectStage` union
- [x] `src/utils/stage.ts` — add label, add to `ALL_STAGES`, exclude from `DEFAULT_STAGES`
- [x] `src/styles/global.css` — add CSS variable + stage-chip + stage-badge rules
- [x] `CLAUDE.md` — update stage values list
- [x] `HELP.md` — update stage values list

## Notes

No content.config.ts change needed — project stage is not in the Zod schema there (only task stage is).
`on_hold` hidden by default (like `abandoned`) — semantically inactive projects.
Color: orange-ish, distinct from `abandoned` (red) and `in_progress` (yellow).
