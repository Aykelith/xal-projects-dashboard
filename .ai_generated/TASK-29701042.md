# Task: Add GitHub Pages deployment

**Request:** Add files required to use GitHub Pages with `/dist` as the web directory.

## Analysis

- `src/content/` and `data/` are gitignored — CI cannot rebuild
- `dist/` is committed — deploy pre-built artifact directly from repo
- GitHub Pages requires Actions workflow to serve from `/dist` (branch deploy only supports root or `/docs`)

## Sub-tasks

- [x] Create `.github/workflows/deploy.yml` — upload `dist/` and deploy to GitHub Pages
